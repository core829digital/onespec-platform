# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
**/.next/
/out/

# apps/status-page lives in its own separate repo (status.onespec.eu) —
# never part of this repo's history, see PROJECT_MEMORY FASE 12.
/apps/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env.local
.env*.local
.env.*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Agent tooling runtime/scratch (keep the checked-in skill catalogues,
# ignore only the machine-generated state).
.claude-flow/
.codex/
.hive-mind/
.swarm/
/memory/
/coordination/
.claude/settings.local.json
.claude/.proven-config-version

# Reference docs (handoff / specs) — not part of the app
*.pdf
/Serramenti-Os*.html
/ONESPEC-*.html
"ONESPEC- V2 - 15.html"

# Sentry Config File
.env.sentry-build-plugin
